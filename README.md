# WentzM3Performance

A Next.js project by **Wentz Designs M3 Division** — specializing in BMW M3 performance parts, catalog management, and community engagement.

---

## 🚀 Features
- **Product Catalog** — served from `/api/products` using `data/products.json`
- **Community Gallery** — submit and view builds via `/api/gallery`
- **Meet Sign-ups** — register for events via `/api/meets`
- **Global Styling** — BMW M-stripe inspired design in `public/styles.css`
- **SEO Ready** — `robots.txt` and `sitemap.xml.js` included
- **Vercel Deployment** — auto-builds on commits to `main`

---

## 📂 Project Structure
